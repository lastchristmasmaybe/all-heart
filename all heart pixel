<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>MyLoveQuest</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<!-- Pixel font -->
<link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">

<style>
* {
  box-sizing: border-box;
  font-family: 'Press Start 2P', cursive;
}

body {
  margin: 0;
  height: 100vh;
  background: linear-gradient(#6a5acd, #ff9ad5);
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

/* GAME SCREEN */
.game {
  width: 360px;
  max-width: 95%;
  background: linear-gradient(#7b6cff, #ffb3dc);
  border: 4px solid #000;
  box-shadow: 8px 8px 0 #000;
  padding: 12px;
  position: relative;
}

/* TOP HUD */
.hud {
  display: flex;
  justify-content: space-between;
  font-size: 10px;
  margin-bottom: 14px;
}

.hud span {
  background: #000;
  color: #fff;
  padding: 4px 6px;
  border-radius: 2px;
}

/* TITLE */
.title {
  text-align: center;
  font-size: 18px;
  color: #ffe066;
  margin-bottom: 16px;
  text-shadow: 2px 2px #000;
}

/* DIALOG BOX */
.box {
  background: #1b1b3a;
  color: #fff;
  border: 3px solid #fff;
  padding: 14px;
  margin-bottom: 16px;
  font-size: 10px;
}

/* QUESTION */
.question {
  color: #ffe066;
  font-size: 11px;
  margin-bottom: 14px;
}

/* BUTTONS */
.buttons {
  display: flex;
  gap: 12px;
  justify-content: center;
}

button {
  font-size: 10px;
  padding: 10px 14px;
  border: 3px solid #000;
  cursor: pointer;
  box-shadow: 4px 4px 0 #000;
}

button:active {
  transform: translate(2px,2px);
  box-shadow: 2px 2px 0 #000;
}

#yes {
  background: #3ddc84;
  color: #000;
}

#no {
  background: #ff595e;
  color: #fff;
  position: relative;
}

/* FOOTER */
.footer {
  text-align: center;
  font-size: 9px;
  margin-top: 16px;
  color: #fff;
}

/* HEARTS */
.hearts {
  position: absolute;
  inset: 0;
  pointer-events: none;
}

.heart {
  position: absolute;
  animation: float 4s linear infinite;
}

@keyframes float {
  0% { transform: translateY(100%); opacity: 0; }
  30% { opacity: 1; }
  100% { transform: translateY(-20%); opacity: 0; }
}
</style>
</head>

<body>

<div class="game" id="game">

  <div class="hud">
    <span>❤️ x3</span>
    <span>🎵 ✉️</span>
  </div>

  <div class="title">MyLoveQuest</div>

  <div class="box">
    Aey Chinnu ❤️<br><br>
    I have something important to ask...
  </div>

  <div class="box">
    <div class="question">Will you be my Valentine?</div>
    <div class="buttons">
      <button id="yes">YES 💖</button>
      <button id="no">ILLA 🙈</button>
    </div>
  </div>

  <div class="footer">Made with ❤️ just for you</div>

  <div class="hearts"></div>
</div>

<script>
const noBtn = document.getElementById("no");
const yesBtn = document.getElementById("yes");
const game = document.getElementById("game");
const hearts = document.querySelector(".hearts");

noBtn.addEventListener("mouseover", () => {
  const x = Math.random() * 120 - 60;
  const y = Math.random() * 80 - 40;
  noBtn.style.transform = `translate(${x}px, ${y}px)`;
});

yesBtn.addEventListener("click", () => {
  game.innerHTML = `
    <div class="title">LEVEL COMPLETE 💖</div>
    <div class="box">
      You unlocked my heart ❤️<br><br>
      Happy Valentine’s Day 🌹<br><br>
      ILOVEYOU
    </div>
    <div class="footer">Achievement Unlocked 🏆</div>
  `;
  spawnHearts();
});

function spawnHearts() {
  for (let i = 0; i < 30; i++) {
    const h = document.createElement("div");
    h.className = "heart";
    h.innerHTML = "❤️";
    h.style.left = Math.random() * 100 + "%";
    h.style.animationDuration = (Math.random() * 2 + 3) + "s";
    hearts.appendChild(h);
    setTimeout(() => h.remove(), 4000);
  }
}
</script>

</body>
</html>
